name: 18-1-reusable-workflows.yaml
on: 
  workflow_call:
    outputs:
      my-output:
        value: ${{ jobs.my-job.outputs.path-output }}
        
jobs:
  my-job:
    runs-on: ubuntu-latest
    outputs:
      path-output: ${{ steps.my-step.outputs.path }}
    steps:
      - name: My Step
        id: my-step
        run: |
          # Do something
          echo "path=my-value" >> "$GITHUB_OUTPUT"